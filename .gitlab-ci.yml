image: alpine:latest

pages:
    script:
    - apk add mdbook python3
    - mdbook build -d public
    artifacts:
        paths:
        - public
    only:
        variables:
        - $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
